# Demeter release rules. Room, Compose, and kotlinx libraries ship consumer keep
# rules; enums used with valueOf() are kept by proguard-android-optimize.txt defaults.
# Add rules here only with a reproduced failure on a minified build.
